name: window-mswindows

on:
  push:
    paths:
      - window/**
  pull_request:
    paths:
      - window/**

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true
      - name: Build
        run: cargo build --verbose
        working-directory: ./window
      - name: Test
        run: cargo test --verbose
        working-directory: ./window
